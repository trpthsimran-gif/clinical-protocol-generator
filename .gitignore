# Environment variables (contains your API key - NEVER commit this)
.env

# Python cache
__pycache__/
*.pyc

# Local databases / vector store (regenerated locally, don't need to be in git)
protocol_cache.db
chroma_db/

# OS files
.DS_Store
Thumbs.db
